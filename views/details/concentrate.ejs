<% if(locals.scheme) { %>
  <%- include('../wrappers/head', {title: scheme.name, login: id}); %>
<% } else { %>
  <%- include('../wrappers/head', {title: 'Concentrate not Found', login: id}); %>
<% } %>
<section>
  <center>
    <h1><%= scheme.name %></h1>
    <img src="<%= scheme.img %>" alt="Picture of <%= scheme.name %>" />
    <p><%= scheme.aroma %></p>
    <p>
      <% if(scheme.desc) { %>
        <%= scheme.desc %>
      <% } else { %>
        Description not provided... Maybe you could provide one!<br>
        <a href="/cct/<%= scheme.name %>/edit?desc_only=true">Edit Description</a>
      <% } %>
    </p>
  </center>
</section>
<%- include('../wrappers/foot'); %>